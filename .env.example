# Copy to .env and fill in. .env is gitignored.
#
# OpenAI API key used by the server to generate dashboard JSON from a
# user prompt. If unset, /api/generate falls back to a deterministic
# keyword-routed stub so the rest of the stack stays usable.
OPENAI_API_KEY=
